<Window x:Class="CodeShellManager.Views.InputBoxDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" SizeToContent="Height" Width="380" MinHeight="170"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1e1e2e" Foreground="#cdd6f4"
        FontFamily="Segoe UI"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" Foreground="#a6adc8" FontSize="12" Margin="0,0,0,6"/>
        <TextBox x:Name="ValueBox" Grid.Row="1"
                 Background="#313244" Foreground="#cdd6f4"
                 BorderBrush="#45475a" BorderThickness="1"
                 Padding="6,4" FontSize="13"
                 CaretBrush="#cdd6f4"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0"
                    Background="#45475a" Foreground="#cdd6f4"
                    BorderThickness="0" Cursor="Hand"
                    Click="Cancel_Click"/>
            <Button Content="OK" Padding="14,6"
                    Background="#89b4fa" Foreground="#1e1e2e"
                    BorderThickness="0" Cursor="Hand"
                    Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
