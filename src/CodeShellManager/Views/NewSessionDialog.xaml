<Window x:Class="CodeShellManager.Views.NewSessionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Session" Height="370" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1e1e2e" Foreground="#cdd6f4"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="Label" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#a6adc8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,3"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#cdd6f4"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderBrush" Value="#45475a"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="Btn" TargetType="Button">
            <Setter Property="Background" Value="#45475a"/>
            <Setter Property="Foreground" Value="#cdd6f4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#585b70"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button" BasedOn="{StaticResource Btn}">
            <Setter Property="Background" Value="#89b4fa"/>
            <Setter Property="Foreground" Value="#1e1e2e"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#b4d0fb"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Working Folder -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Working Folder" Style="{StaticResource Label}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="FolderBox" Grid.Column="0" Margin="0,0,6,0"/>
                <Button Grid.Column="1" Content="Browse…" Style="{StaticResource Btn}"
                        Click="BrowseFolder_Click"/>
            </Grid>
        </StackPanel>

        <!-- Command -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Command" Style="{StaticResource Label}"/>
            <ComboBox x:Name="CommandCombo" SelectionChanged="CommandCombo_SelectionChanged">
                <ComboBoxItem Content="claude" Tag="claude"/>
                <ComboBoxItem Content="claude --continue" Tag="claude --continue"/>
                <ComboBoxItem Content="claude --model claude-opus-4-6" Tag="claude --model claude-opus-4-6"/>
                <ComboBoxItem Content="claude --dangerously-skip-permissions" Tag="claude --dangerously-skip-permissions"/>
                <ComboBoxItem Content="codex" Tag="codex"/>
                <ComboBoxItem Content="gh copilot suggest" Tag="gh copilot suggest"/>
                <ComboBoxItem Content="pwsh" Tag="pwsh"/>
                <ComboBoxItem Content="cmd" Tag="cmd"/>
                <ComboBoxItem Content="[custom]" Tag="custom"/>
            </ComboBox>
        </StackPanel>

        <!-- Custom args (shown when custom selected) -->
        <StackPanel Grid.Row="2" x:Name="CustomArgsPanel" Visibility="Collapsed" Margin="0,0,0,12">
            <TextBlock Text="Custom Command + Args" Style="{StaticResource Label}"/>
            <TextBox x:Name="CustomArgsBox" Tag="e.g. claude --model claude-haiku-4-5"/>
        </StackPanel>

        <!-- Session Name -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Session Name (optional)" Style="{StaticResource Label}"/>
            <TextBox x:Name="NameBox"/>
        </StackPanel>

        <!-- Group -->
        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="Group" Style="{StaticResource Label}"/>
            <ComboBox x:Name="GroupCombo"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource Btn}" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Start Session" Style="{StaticResource PrimaryBtn}" Click="Start_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
